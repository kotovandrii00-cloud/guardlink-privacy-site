# GuardLink Privacy Site

Static GitHub Pages site for the GuardLink privacy policy.

Custom domain:

```text
privacy.guardlink-app.com
```

DNS record to add:

```text
CNAME  privacy  <github-username>.github.io
```

Do not change the existing `livekit.guardlink-app.com` DNS record.
